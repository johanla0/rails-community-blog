---
en:
  activerecord:
    models:
      post: post
      post_comment: comment
  attributes:
    body: Text
  errors:
    messages:
      blank: should not be empty
  helpers:
    submit:
      post:
        create: Publish %{model}
      post_comment:
        create: Publish %{model}
